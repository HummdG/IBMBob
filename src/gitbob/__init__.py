"""GitBob — a natural-language Git copilot for IBM Bob.

GitBob is an MCP server. IBM Bob is the agent (the brain); GitBob is the
hands: it exposes safe, structured git tools that Bob calls while it does
the natural-language reasoning, planning, and recovery.
"""

__version__ = "0.1.0"
