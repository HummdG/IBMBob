# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Virtualenv (machine-specific; recreate with `python -m venv .venv`)
.venv/

# Generated, machine-specific demo state. Regenerate locally with:
#   pipx install gitbob          (or: pipx install .)
#   python demo/setup_demo_repo.py
# demo-repo is fully generated — it must NOT be tracked (it was an
# embedded git repo / gitlink; run `git rm --cached demo-repo` once).
demo-origin.git/
.demo-teammate/
demo-repo/

# OS
.DS_Store
Thumbs.db
